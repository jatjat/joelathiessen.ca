apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "joelathiessen-ca.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "joelathiessen-ca.name" . }}
    helm.sh/chart: {{ include "joelathiessen-ca.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "joelathiessen-ca.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
